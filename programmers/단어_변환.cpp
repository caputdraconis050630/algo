#include <bits/stdc++.h>
using namespace std;

int word_len;

int dfs(t)
{
    if (begin.compare(target) == 0)
    {
        return ()
    }
}

int solution(string begin, string target, vector<string> words) {
    int answer = 0;



       
    return answer;
}