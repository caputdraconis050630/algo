#include <bits/stdc++.h>
using namespace std;

int N;
string board[65];

int main(void){
    ios::sync_with_stdio(0);
    cin.tie(NULL);

    cin>>N;
    for(int i=0;i<N;i++){
        cin>>board[i];
    }
}