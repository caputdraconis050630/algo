#include <bits/stdc++.h>
using namespace std;

int L, R, C;
/*
    L : 층 수
    R : 한 층의 행
    C : 한 층의 열
*/

char board[35][35][35];

int main(void)
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    cin >> L >> R >> C;

    while (!(L == 0 && R == 0 && C == 0))
    {
        for (int i = 0; i < L; i++)
        {
        }

        cin >> L >> R >> C; //다음 테스트 케이스를 위한 입력
    }
}