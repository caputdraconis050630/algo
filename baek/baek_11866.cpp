#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    int N, K;
    cin >> N >> K;

    vector<int> v(N);


    // 벡터 초기화
    for (int i = 0; i < N; i++)
    {
        v.push_back(i + 1);
    }
    cout << "<";


}