#include <bits/stdc++.h>
using namespace std;

int main(void)
{
    ios::sync_with_stdio(0);
    cin.tie(NULL);

    int tc;
    cin >> tc;

    while (tc--)
    {
        double tmp;
        cin >> tmp;

    
        printf("$%0.2lf\n", tmp*0.8);
    }
}