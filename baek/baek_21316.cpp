#include <bits/stdc++.h>
using namespace std;

int main()
{
    int N;
    int K;
    int small_jump[N - 1];
    int big_jump[N - 1];

    scanf("%d", &N);

    for (int i = 0; i < N - 1; i++)
    {
        scanf("%d %d", &small_jump[i], &big_jump[i]);
    }

    scanf("%d", &K);

    


}