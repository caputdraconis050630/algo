#include <iostream>
#include <string>
#include <cmath>

using namespace std;

int main(void) {
	int T;
	cin >> T;

	for (int t=0; t<T; t++) {
		string ans = "";
		string N, x, y;
		cin >> N >> x >> y;
		

	}
}